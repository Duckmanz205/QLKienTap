# requirements.txt — AI Service Dependencies
# Cấu hình các thư viện cho AI Microservice (OCR & AI Grader)

# === Web Framework ===
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*      # Hỗ trợ nhận file PDF upload qua API

# === Machine Learning / AI Integration ===
openai==1.52.*               # Giao tiếp với Qwen Cloud API (Compatible mode)
httpx==0.27.*                # Ép phiên bản HTTP client tương thích với OpenAI SDK

# === Document & Image Processing (OCR Vision) ===
PyMuPDF==1.24.*              # Đọc và bóc tách PDF (tên import là fitz)
Pillow==10.4.*               # Xử lý hình ảnh cắt từ PDF trước khi gửi OCR

# === Data Validation & Parsing ===
pydantic==2.9.*              # Ràng buộc cấu trúc dữ liệu JSON Barem

# === Utilities ===
python-dotenv==1.1.*         # Đọc biến môi trường